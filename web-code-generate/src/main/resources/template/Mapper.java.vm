package ${package}.${moduleName}.mapper;

import ${package}.${moduleName}.entity.${className}DO;
import com.baomidou.mybatisplus.mapper.BaseMapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Mapper extends BaseDao<${className}DO> {
	
}
